import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const PolicyViewerScreen = () => {
  return (
    <View>
      <Text>PolicyViewerScreen</Text>
    </View>
  );
};

export default PolicyViewerScreen;

const styles = StyleSheet.create({});
